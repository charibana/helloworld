#include <iostream>
using namespace std;
int main()
{
    int n;
    int max=0;
    int sum=0;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int num;
        cin>>num;
        sum+=num;
        if(sum>max) max=sum;
        if(sum<0) sum=0;

    }

    cout<<max;
    return 0;
}
